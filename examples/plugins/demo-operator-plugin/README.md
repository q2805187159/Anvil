# Demo Operator Plugin

This local plugin exists so the Ops Console can render a real catalog entry and install it without network access.

It provides one deterministic inline tool, one resource, one prompt template, and one sample skill root.
