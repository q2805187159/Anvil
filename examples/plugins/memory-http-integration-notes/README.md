# Memory HTTP Integration Notes

This example is documentation-only. It shows where a team can keep notes for
HTTP memory integrations while keeping HCMS as Anvil's active memory engine.

External memory services should integrate through normal tools, MCP servers, or
gateway clients. They do not replace HCMS recall, capture, lifecycle, or version
history.
